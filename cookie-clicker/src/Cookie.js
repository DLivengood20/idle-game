import React from 'react';

const Cookie = ({ count }) => {
  return (
    <div>
      <img src="cookie.png" alt="Cookie" />
      <div>Cookies: {count}</div>
    </div>
  );
};

export default Cookie;
